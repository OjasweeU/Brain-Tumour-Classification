Place the trained checkpoint from the notebook or retraining script here as `model.keras`.

The deployment app will also accept `model.h5`, but `model.keras` is the default for current Keras versions.
